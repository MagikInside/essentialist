import { UnshiftFakeTripPipe } from './unshift-fake-trip.pipe';

describe('UnshiftFakeTripPipe', () => {
  it('create an instance', () => {
    const pipe = new UnshiftFakeTripPipe();
    expect(pipe).toBeTruthy();
  });
});
